<!DOCTYPE html>
<html>
<head>
  <title>SwapX</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <link href="https://unpkg.com/nes.css@latest/css/nes.min.css" rel="stylesheet" />
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <h2 class="logo-text"><a href="/">SwapX</a></h2>
      <div>
        <ul class="navbar-nav text-right">
          <li class="nav-item mr-auto nav-link">
            <%= link_to 'My Profile', "/" %>
          </li>
          <li class="nav-item mr-auto nav-link">
            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#itemModal">
              Add Item
            </button>
          </li>
          <%- if current_user -%>
          <li class="nav-item mr-auto">
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, :class => "nav-link" %>
          </li>
          <%- else -%>
          <li class="nav-item btn btn-primary btn-sm sign-btns">
            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
              Sign Up
            </button>
          </li>
          <li class="nav-item btn btn-primary btn-sm sign-btns">
            <%= link_to 'Sign In', new_user_session_path, :class => "nav-link" %>
          </li>
          <%- end -%>
        </ul>
      </div>
    </div>
  </nav>

  <%- flash.each do |key, value| -%>
    <div class="flash flash-<%= key %>">
      <%= value %>
    </div>
  <%- end -%>

  <%= yield %>

  <%= javascript_include_tag 'application' %>

  <%- if Rails.env.development? || Rails.env.test? -%>
    <%= javascript_tag do %>
      window.railsEnv = '<%= Rails.env %>'
    <%- end -%>
  <%- end -%>
  <%= javascript_pack_tag 'application' %>

  <%= yield :extra_footer %>
</body>
</html>
